# Tasks: 可配置坐席工作流与工作台编排

## Checklist

- [x] 梳理并确认默认工作流方案，包括运营链路 `L1->L2` 与系统报障链路 `L1->L2->L3`
- [x] 梳理客户转人工的意图分类，并确认每类意图对应的目标工作流和入口阶段
- [x] 设计并实现工作流模板、阶段节点的数据模型与后端持久化结构
- [x] 扩展 AI 预处理或新增转人工分析接口，返回 `intentCode`、`targetWorkflowCode`、`targetStageCode`、`confidence`
- [x] 设计并实现意图到工作流的路由规则配置，支持启停和默认回退
- [x] 扩展 `ServiceTask` 模型，增加 `workflowCode`、`currentStageCode`、`currentStageOrder` 等字段
- [x] 调整客户侧转人工流程，使任务创建前先完成意图识别和工作流路由
- [x] 调整任务流转逻辑，使升级/转派按工作流配置推进到下一阶段
- [x] 为 `GET /api/tasks` 增加按工作流与阶段查询的能力，并兼容旧 `level` 参数
- [x] 新增工作流配置管理接口：工作流模板查询、新增、编辑、启停
- [x] 新增转人工分析与人工任务创建接口，或兼容扩展现有接口
- [x] 抽象工作台布局配置模型，包括区域、组件、顺序、显隐和组件参数
- [x] 实现工作台配置接口，并支持按“工作流 + 阶段”获取有效布局
- [x] 将 `AgentWorkstationView` 改造为配置驱动容器，支持动态渲染组件区域
- [x] 建立 widget registry，接入首版组件：任务池、我的任务、聊天区、客户信息、保单信息、AI Copilot、历史记录、报障入口、阶段轨迹、知识卡片
- [x] 新增管理端配置页面，用于维护工作流模板和工作台布局
- [x] 增加布局校验与默认回退机制，保证关键组件缺失时仍可进入默认工作台
- [x] 调整顶部演示导航文案，使其与工作流阶段命名一致，并补充报障链路 `L2 / L3` 的直接入口
- [x] 完成前后端联调与回归测试，覆盖转人工识别、运营链路、报障链路和多阶段流转
- [x] 审批通过并完成实现后，同步更新相关 `openspec/specs/` 与 `PRD.md`
