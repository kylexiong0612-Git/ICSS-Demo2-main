# ZHIPU_API_KEY: 智谱 AI（GLM-4-Flash）API 密钥。
# 此 Key 由 Vite 开发代理在服务端注入，不会打包进前端 bundle。
# 生产环境需在 BFF 层代理 /api/ai 路径并持有此 Key。
ZHIPU_API_KEY="your-zhipu-api-key"

# VITE_API_BASE_URL: 后端 API 基础地址（可选，默认为空）。
VITE_API_BASE_URL=""
